version: '2'

services:
  django-service:
    build: ./
    #image: django-devf
    ports:
      - "8000:8000"
    depends_on:
      - postgres-docker

  postgres-docker:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=root